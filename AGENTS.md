# Repository Instructions

- The primary user works in Chinese. Respond in Chinese unless the user asks otherwise.
- At the end of every work session, update `docs/WORKLOG.md`.
- At the end of every work session, update `docs/KNOWLEDGE_BASE.md` when any reusable project knowledge, architecture decision, deployment note, convention, or gotcha was learned.
- `docs/WORKLOG.md` should record what changed, important files, commands run, verification/deployment results, and known risks.
- `docs/KNOWLEDGE_BASE.md` should record durable knowledge, not every temporary action.
- Never write plaintext admin passwords, API keys, ST values, AT values, Google cookies, proxy credentials, or SSH private material into docs. Mask secrets if they must be referenced.
- Preserve unrelated user changes in the working tree. Do not revert files unless the user explicitly asks.
